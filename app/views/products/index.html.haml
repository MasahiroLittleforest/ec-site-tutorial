%h2.center
  商品一覧
.products
  .row
    - @products.each do |product|
      = link_to product_path(product) do
        .col.s12.m6
          .card
            .card-image
              = image_tag product.image.url || "sample.png"
              %span.card-title
                #{product.name}(#{product.display_price})
            .card-content
              %p
                = product.description
              %p